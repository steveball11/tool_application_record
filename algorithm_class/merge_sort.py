# find base case and recursive case
# in base case, directly uotput the result
# recursive case (n>1)
# divide the list to two list
# sort each sublist recursively
# merge the two sorted lists
# idea : merge two sorted